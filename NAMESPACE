useDynLib(EBImage)
export(
    Image, read.image, ping.image,

    blur, contrast, despeckle, edge, enhance, equalize,
    colorGamma, gaussFilter, medianFilter, mod, reduceNoise,
    rotate, sample.image, zoom.image, segment,
    shade, sharpen, solarize, trim,
    spread, unsharpMask, noise, 
    
    distMap, thresh, ws, wsPaint, wsImages, 
    erode, dilate, mopen, mclose,

    draw, DrawableCircle, DrawableLine, DrawableRect, DrawableEllipse
)
exportClasses(
    Image,

    Drawable, DrawableCircle, DrawableLine, DrawableRect, DrawableEllipse,

    # DEPRECATE
    Image2D, Image3D
)
exportMethods(
    assert, copy, write.image, display, channels, normalize,
    as.array, summary, "[", show, print, plot.image,

    toGray, toRGB, toX11char, toRed, toGreen, toBlue, getRed, getGreen, getBlue, 
    add2RGB, sub2RGB, scale2RGB
)
